const unionWith = (...params) => {
	/*const arrays = params.slice(0, params.length - 1)
	const comparator = params[params.length - 1]

	// 连接所有数组
	const allArray = arrays.reduce((allArray, array) => allArray.concat(array))*/

}

export defalut unionWith